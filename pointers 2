#include <stdio.h>
int main() {
    int a[] = {1, 3, 4, 6, 8}, sum = 10, i = 0, j = 4;
    while (i < j) {
        if (a[i] + a[j] == sum) {
            printf("Pair: %d %d", a[i], a[j]);
            return 0;
        } 
        else if (a[i] + a[j] < sum) i++;
        else j--;
    }
}
